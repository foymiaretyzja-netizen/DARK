// ai.js
export function updateGhost(ghostMesh, playerCamera, delta) {
    // Future AI Logic goes here.
    // Example: Make the ghost slowly hover towards the player if they look away
    
    /* const speed = 2.0;
    const direction = new THREE.Vector3();
    direction.subVectors(playerCamera.position, ghostMesh.position).normalize();
    // ghostMesh.position.addScaledVector(direction, speed * delta);
    */
}
